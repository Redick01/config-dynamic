server:
  port: 8890

spring:
  application:
    name: cloud-zk
  cloud:
    zookeeper:
      connect-string: 127.0.0.1:2181
      config:
        root: /configserver/dev
      enabled: true
      watcher:
        enabled: true
  profiles:
    active: dev
